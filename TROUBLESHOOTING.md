# 🔧 Решение проблем (Troubleshooting)

## ❌ Проблемы с GitHub Actions

### Деплой не запускается автоматически

**Симптомы:** После push ничего не происходит во вкладке Actions

**Решение:**
1. Проверьте, что файлы `.github/workflows/deploy.yml` существуют в репозитории
2. Откройте Settings → Actions → General
3. Убедитесь, что Actions разрешены для репозитория
4. Проверьте, что вы пушите в ветку `main` или `master`

### Ошибка "Deploy to GitHub Pages" failed

**Симптомы:** Job `deploy` падает с ошибкой

**Решение:**
1. Settings → Pages → Source должен быть "GitHub Actions"
2. Проверьте права доступа в Settings → Actions → General
3. Убедитесь, что в workflow есть правильные permissions:
   ```yaml
   permissions:
     contents: read
     pages: write
     id-token: write
   ```

### Ошибка при сборке (build failed)

**Симптомы:** Job `build` падает на этапе `flutter build web`

**Решение:**

1. **Проверьте локально:**
   ```bash
   flutter clean
   flutter pub get
   flutter build web --release
   ```

2. **Если локально работает, но на GitHub нет:**
   - Проверьте версию Flutter в workflow (сейчас используется `stable`)
   - Убедитесь, что все зависимости совместимы

3. **Посмотрите лог ошибки:**
   - Откройте Actions → выберите failed run
   - Раскройте шаг с ошибкой
   - Скопируйте полный текст ошибки

## 🌐 Проблемы с отображением сайта

### Сайт не открывается (404)

**Симптомы:** По адресу `username.github.io/repo-name/` показывается 404

**Решение:**
1. Подождите 2-3 минуты после деплоя
2. Проверьте, что деплой завершился успешно (зеленая галочка ✅)
3. Settings → Pages должен показывать "Your site is live at..."
4. Проверьте правильность `base-href` в deploy.yml:
   ```yaml
   # Должно совпадать с именем репозитория
   flutter build web --release --base-href "/your-repo-name/"
   ```

### Главная страница загружается, но нет стилей/картинок

**Симптомы:** Страница есть, но выглядит неправильно, изображения не грузятся

**Причина:** Неправильный `base-href`

**Решение:**

1. **Если репозиторий называется `rabay_landing`:**
   ```yaml
   --base-href "/rabay_landing/"
   ```

2. **Если это главный сайт (username.github.io):**
   ```yaml
   --base-href "/"
   ```

3. **После изменения:**
   ```bash
   git add .github/workflows/deploy.yml
   git commit -m "Fix base-href"
   git push origin main
   ```

### Скриншоты не отображаются

**Причина:** Assets не добавлены или неправильный путь

**Решение:**

1. Проверьте `pubspec.yaml`:
   ```yaml
   flutter:
     assets:
       - assets/images/screens/
   ```

2. Убедитесь, что файлы существуют:
   ```bash
   ls assets/images/screens/
   # Должно показать: accounts.jpg, main_budget.jpg, savings.jpg, shopping_list.jpg
   ```

3. Если файлы есть, пересоберите:
   ```bash
   flutter clean
   flutter pub get
   flutter build web --release
   ```

## 🔗 Проблемы с ссылками

### Кнопки App Store/Google Play не работают

**Причина:** URL не обновлены в константах

**Решение:**

Откройте `lib/core/constants/app_constants.dart`:
```dart
static const String appStoreUrl = 'https://apps.apple.com/app/your-app-id';
static const String googlePlayUrl = 'https://play.google.com/store/apps/details?id=your.package';
```

### Социальные ссылки не работают

Аналогично обновите:
```dart
static const String instagramUrl = 'https://instagram.com/your_profile';
static const String facebookUrl = 'https://facebook.com/your_page';
static const String telegramUrl = 'https://t.me/your_channel';
```

## 💻 Локальные проблемы

### Flutter build web не работает

**Ошибка:** Package not found или compilation error

**Решение:**
```bash
# 1. Очистите кэш
flutter clean

# 2. Обновите Flutter
flutter upgrade

# 3. Переустановите зависимости
rm -rf .dart_tool
rm pubspec.lock
flutter pub get

# 4. Попробуйте снова
flutter build web --release
```

### Ошибки анализа кода

**Решение:**
```bash
# Запустите анализ
flutter analyze

# Посмотрите конкретные ошибки и исправьте их
# Или временно отключите строгие правила в analysis_options.yaml
```

## 📱 Проблемы с адаптивностью

### На мобильных выглядит неправильно

**Решение:**
1. Откройте DevTools в браузере (F12)
2. Включите режим мобильного устройства
3. Проверьте разные размеры экрана
4. Виджеты должны адаптироваться через `isDesktop`, `isTablet`, `isMobile`

## 🔄 Старая версия после деплоя

**Симптомы:** Изменения не видны на сайте, хотя деплой успешен

**Решение:**
1. Очистите кэш браузера (Ctrl+Shift+R или Cmd+Shift+R)
2. Откройте в режиме инкогнито
3. Проверьте, что новый commit действительно задеплоился:
   - Actions → последний run должен быть с вашим последним commit

## 🆘 Все еще не работает?

1. **Проверьте логи GitHub Actions:**
   - Actions → выберите failed run
   - Раскройте каждый шаг
   - Скопируйте текст ошибок

2. **Создайте Issue в репозитории:**
   - Опишите проблему
   - Приложите скриншоты
   - Приложите логи из Actions

3. **Проверьте примеры:**
   - Посмотрите успешные деплои других Flutter Web проектов
   - Сравните конфигурацию workflow файлов

---

**Не нашли решение?** Откройте Issue с описанием проблемы и логами.
